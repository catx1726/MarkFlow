# Release Notes — v0.9.1 (2026-09-13)

> 补丁版：品牌图标清理（v0.9.0 的发布物构建于清理之前，AMO 版本不可变，按 0.9.1 修复版分发）。

## 中文

- 清理 popup 界面残留的旧品牌图标（像素风格，已弃用两代）
- 删除仓库内无引用的旧蓝色 logo 资产（`src/assets/logo.svg`）

## English

- Removed the deprecated pixel-style brand icon left in the popup header
- Dropped the orphaned legacy blue logo asset (`src/assets/logo.svg`)

## Driver 发布步骤

1. `npm run build` → 打包 `extension/` 为 `extension-chromium.zip`
2. `gh release create v0.9.1 --title "v0.9.1 – 图标清理" --notes-file docs/release-notes/v0.9.1.md` + 上传 zip
3. AMO 后台 → 新版本 0.9.1 → 上传 → 提交审核（0.9.0 不动，用户自动收到 0.9.1 更新）
4. 可选：在 v0.9.0 release 说明补一句「popup 图标残留，0.9.1 已修复」
