# Task 003: 内容页 B~E（b/c/d/e.html）+ 跨页回跳联调

## Context

引擎在 A 页验证通过。本 task 按同一骨架契约产出 B~E 四页（结构逐字复用 A 页，仅换：`<title>`、`data-page`、页签高亮、mono 章节标签、正文选段），并联调跨页回跳闭环。

## Target Logic

| 文件 | `data-page` | 章节标签 | 选段（按 `_index.md` 契约） |
| :--- | :--- | :--- | :--- |
| `docs/try/b.html` | `b` | `体验 / 笔记 B · 以手、物件和眼神为主题的电影` | B 行 4 段（含「无意识行为」「节奏源于精确」） |
| `docs/try/c.html` | `c` | `体验 / 笔记 C · 触及神秘` | C 行 4 段（含「必要的影像」「影像之间的关系」） |
| `docs/try/d.html` | `d` | `体验 / 笔记 D · 诗歌与真实是姐妹` | D 行 4 段（含「创造首先就是删减」「无意识行为 P172」） |
| `docs/try/e.html` | `e` | `体验 / 笔记 E · 个人总结` | E 行（含「心跳是什么？内心、真实情感、无意识、自发」、加缪转折、结语「不演戏，听心跳。」） |

每段 `<p data-para>` 索引从 0 连续编号；页签导航 `A B C D E` 分别指向 `index.html / b.html / c.html / d.html / e.html`。

## Verification

```bash
# 手工（跨页闭环，即录屏动线）：
# 1. A 页标「心结」（标签：节奏）→ 页签翻到 B → 标「节奏源于精确」（同标签）→ 翻到 E → 标「心跳是什么…」（同标签）
# 2. 侧栏「节奏」分组下出现 3 条，分属 笔记A/笔记B/笔记E
# 3. 在 E 页点击 A 页条目 → 跳转 index.html#mark-<id>，滚动居中 + 琥珀脉冲，hash 被清除
# 4. 刷新 E 页 → 三页标记均仍在侧栏；重置后全部清空
# 5. 四页明暗切换、提示条、侧栏表现与 A 页一致
```
