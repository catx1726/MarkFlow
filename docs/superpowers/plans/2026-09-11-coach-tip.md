# Coach Tip（首次引导浮层）Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 首次未按 Alt 划词时，在选区旁显示一次性轻提示「按住 Alt 划词即可标记」，把核心手势教给新用户。

**Architecture:** 纯函数判定（`src/logic/coachTip.ts`）+ Shadow DOM 内第三个 Vue 视图（`CoachTip.vue`，与 Tooltip/DisambiguationModal 同构）+ `processSelection` 末尾分支触发 + `settings.coachTipDone` 标志（显示即置位 / Alt 成功标记即置位）。浮层 `pointer-events: none` 零交互，消失三通道（6s 超时 / 任意 mousedown / 滚动）全部由组件自管理，宿主代码零侵入。

**Tech Stack:** Vue 3 (`createApp` 手动挂载) / UnoCSS（px 任意值，Shadow DOM 内禁 rem）/ vitest + @vue/test-utils (jsdom) / webext-bridge storage。

**Spec:** `docs/superpowers/specs/2026-09-11-coach-tip-design.md`

**对 Spec 的一处细化（执行层决策）:** Spec §3.6 的单 key `coachTip.hint`（`{key}` 插值）改为 `hintPrefix` / `hintSuffix` 两个 key + 中间独立 `<kbd>` 元素——键帽需要独立 amber 样式，单 key 插值无法对占位符局部着色。先例：`options.quickStartPrefix/Suffix` + `<kbd>`（Options.vue:364-365）。

**环境注意（Windows / 本仓库）:**
- 测试单跑：`npx vitest run src/tests/<name>.spec.ts`（勿用 `npm run test`——它是 watch 模式入口 `vitest test`，Agent 环境下应始终带 `run`）
- Shell 为 PowerShell 5.1，链式命令用 `;` + `if ($?)`，不支持 `&&`
- 提交前 `git status` 全量核对（PR #69 事故教训：路径枚举遗漏）

---

### Task 1: 纯函数模块 `coachTip.ts`（TDD 红-绿）

**Files:**
- Create: `src/tests/coachTip.spec.ts`
- Create: `src/logic/coachTip.ts`

- [ ] **Step 1: 写失败测试**

创建 `src/tests/coachTip.spec.ts`：

```ts
import { describe, expect, it } from 'vitest'
import { coachKeyLabel, shouldShowCoachTip } from '~/logic/coachTip'

describe('shouldShowCoachTip', () => {
  const base = { altKey: false, isCollapsed: false, onMarkElement: false, coachTipDone: false }

  it('普通划词（未按 Alt、非折叠、未置位）→ 显示', () => {
    expect(shouldShowCoachTip(base)).toBe(true)
  })

  it('已置位 → 不显示（严格一次）', () => {
    expect(shouldShowCoachTip({ ...base, coachTipDone: true })).toBe(false)
  })

  it('按住 Alt 的划词 → 不显示（已是核心手势路径，Tooltip 接管）', () => {
    expect(shouldShowCoachTip({ ...base, altKey: true })).toBe(false)
  })

  it('折叠选区（纯点击未划词）→ 不显示', () => {
    expect(shouldShowCoachTip({ ...base, isCollapsed: true })).toBe(false)
  })

  it('点击已有标记 → 不显示（走既有 Tooltip 路径）', () => {
    expect(shouldShowCoachTip({ ...base, onMarkElement: true })).toBe(false)
  })
})

describe('coachKeyLabel', () => {
  it('非 Mac 平台显示 Alt', () => {
    expect(coachKeyLabel(false)).toBe('Alt')
  })

  it('Mac 平台显示 ⌥ Option', () => {
    expect(coachKeyLabel(true)).toBe('⌥ Option')
  })
})
```

- [ ] **Step 2: 运行确认失败**

Run: `npx vitest run src/tests/coachTip.spec.ts`
Expected: FAIL——`Failed to resolve import "~/logic/coachTip"`（模块不存在）

- [ ] **Step 3: 最小实现**

创建 `src/logic/coachTip.ts`：

```ts
/**
 * Coach Tip（首次引导浮层）纯逻辑
 * Spec: docs/superpowers/specs/2026-09-11-coach-tip-design.md
 *
 * 触发条件：用户划了词（非折叠选区）但未按住 Alt，且从未被引导过。
 * 「已引导」= 显示过一次 Coach Tip（显示即置位）
 *          或已成功用 Alt 创建过标记（学会即标记，见 UIManager.createHighlight）。
 */

export interface CoachTipCondition {
  altKey: boolean
  isCollapsed: boolean
  onMarkElement: boolean
  coachTipDone: boolean
}

export function shouldShowCoachTip(condition: CoachTipCondition): boolean {
  const { altKey, isCollapsed, onMarkElement, coachTipDone } = condition
  return !altKey && !isCollapsed && !onMarkElement && !coachTipDone
}

/** 键帽文案：Mac 上 Alt 键实际是 Option（⌥），按平台习惯展示 */
export function coachKeyLabel(isMac: boolean): string {
  return isMac ? '⌥ Option' : 'Alt'
}
```

- [ ] **Step 4: 运行确认通过**

Run: `npx vitest run src/tests/coachTip.spec.ts`
Expected: PASS（7 个用例全绿）

- [ ] **Step 5: 提交**

```powershell
git add src/logic/coachTip.ts src/tests/coachTip.spec.ts; if ($?) { git commit -m "feat(coach-tip): 新增 shouldShowCoachTip/coachKeyLabel 纯函数（TDD）" }
```

---

### Task 2: settings 字段 + i18n 双语词条

**Files:**
- Modify: `src/logic/settings.ts:19`（theme 行后）
- Modify: `src/logic/i18n/locales/zh-CN.ts:94` 与 `:160`（options 组 + tooltip 组后）
- Modify: `src/logic/i18n/locales/en.ts:94` 与 `:160`（同位置）

- [ ] **Step 1: settings 增加 coachTipDone**

`src/logic/settings.ts` —— 在 `theme: 'auto' as ...` 行之后（`}` 之前）插入：

```ts
  coachTipDone: false, // 首次引导浮层已展示或已学会 Alt 手势（本地偏好，不同步）
```

- [ ] **Step 2: zh-CN 词条**

`src/logic/i18n/locales/zh-CN.ts`：

(a) `themeDark: '深色',` 行后插入 options 组词条：

```ts
    coachTipLabel: '首次使用引导',
    coachTipDesc: '安装后首次普通划词时，会提示「按住 Alt 划词即可标记」的核心手势。',
    coachTipStatusShown: '已显示',
    coachTipStatusNotShown: '未显示',
    coachTipReshow: '重新显示',
```

(b) `tooltip: { ... }` 组的收尾 `},`（`confirmHighlight: '确认高亮',` 之后）插入新组：

```ts
  coachTip: {
    hintPrefix: '按住 ',
    hintSuffix: ' 划词即可标记',
  },
```

- [ ] **Step 3: en 词条（同构，缺 key 编译期报错）**

`src/logic/i18n/locales/en.ts`：

(a) `themeDark: 'Dark',` 行后插入：

```ts
    coachTipLabel: 'First-use guide',
    coachTipDesc: 'After install, the first plain text selection shows a tip about the "Hold Alt to highlight" gesture.',
    coachTipStatusShown: 'Shown',
    coachTipStatusNotShown: 'Not shown',
    coachTipReshow: 'Show again',
```

(b) `confirmHighlight: 'Highlight',` 之后的 `},` 后插入：

```ts
  coachTip: {
    hintPrefix: 'Hold ',
    hintSuffix: ' while selecting to highlight',
  },
```

- [ ] **Step 4: 验证（i18n 同构测试 + typecheck）**

Run: `npx vitest run src/tests/i18n.spec.ts`
Expected: PASS（「en 与 zh-CN 字典结构同构」用例校验新 key 双侧齐全）

Run: `npm run typecheck`
Expected: 0 error

- [ ] **Step 5: 提交**

```powershell
git add src/logic/settings.ts src/logic/i18n/locales/zh-CN.ts src/logic/i18n/locales/en.ts; if ($?) { git commit -m "feat(coach-tip): settings.coachTipDone 标志 + 中英双语词条" }
```

---

### Task 3: `CoachTip.vue` 组件（TDD 红-绿）

**Files:**
- Create: `src/tests/coachTipComponent.spec.ts`
- Create: `src/contentScripts/views/CoachTip.vue`

组件设计要点（执行者必读）：
- 两阶段渲染：`visible=true` 时先 `visibility:hidden` 挂载测量 `getBoundingClientRect()`，`computeTooltipPosition` 定位完成后 `isPositioned=true` 才可见（抄 Tooltip.vue:205-228 模式，防闪跳）
- 消失三通道全部组件自管理：`window` capture 级 `mousedown` / `scroll` 监听 + 6s `setTimeout`，`hide()` 时统一清理（`onUnmounted` 兜底）
- `pointer-events: none`：纯提示零交互，点击穿透宿主页
- 样式遵守 Shadow DOM 约定：**只用 px 任意值**（`text-[12px]`、`px-[12px]`），禁 rem（宿主 `<html>` font-size 会污染，见 ui.ts:34-37 注释）
- `<kbd>` 键帽 amber 配色沿品牌（参照 `bg-amber-100 dark:bg-amber-900/40 text-amber-800 dark:text-amber-300`，Tooltip 标签选中态先例 Tooltip.vue:348）

- [ ] **Step 1: 写失败测试**

创建 `src/tests/coachTipComponent.spec.ts`：

```ts
import { mount } from '@vue/test-utils'
import { nextTick } from 'vue'
import { afterEach, beforeEach, describe, expect, it, vi } from 'vitest'
import CoachTip from '~/contentScripts/views/CoachTip.vue'

// jsdom 无布局，getBoundingClientRect 返回 0，computeTooltipPosition 对 0 尺寸照常钳制不抛错
const anchor = { top: 100, left: 100, width: 200, height: 20 }

// stub 掉 Transition，避免 leave 动画的异步钩子让 v-if 移除滞后
const mountOptions = { global: { stubs: ['transition'] } } as const

type Exposed = { show: (a: typeof anchor) => Promise<void>, hide: () => void }

async function showTip(wrapper: ReturnType<typeof mount>) {
  await (wrapper.vm as unknown as Exposed).show(anchor)
  await nextTick()
}

describe('CoachTip.vue', () => {
  beforeEach(() => {
    vi.useFakeTimers()
  })

  afterEach(() => {
    vi.useRealTimers()
  })

  it('show() 后可见，文案含 Alt 键帽与提示语', async () => {
    const wrapper = mount(CoachTip, mountOptions)
    await showTip(wrapper)
    expect(wrapper.find('.coach-tip').exists()).toBe(true)
    expect(wrapper.text()).toContain('Alt')
    expect(wrapper.text()).toContain('划词即可标记')
  })

  it('hide() 后不可见', async () => {
    const wrapper = mount(CoachTip, mountOptions)
    await showTip(wrapper)
    ;(wrapper.vm as unknown as Exposed).hide()
    await nextTick()
    expect(wrapper.find('.coach-tip').exists()).toBe(false)
  })

  it('任意 mousedown 立即隐藏（点击穿透通道）', async () => {
    const wrapper = mount(CoachTip, mountOptions)
    await showTip(wrapper)
    window.dispatchEvent(new MouseEvent('mousedown'))
    await nextTick()
    expect(wrapper.find('.coach-tip').exists()).toBe(false)
  })

  it('页面滚动立即隐藏（防脱锚通道）', async () => {
    const wrapper = mount(CoachTip, mountOptions)
    await showTip(wrapper)
    window.dispatchEvent(new Event('scroll'))
    await nextTick()
    expect(wrapper.find('.coach-tip').exists()).toBe(false)
  })

  it('6s 超时自动隐藏', async () => {
    const wrapper = mount(CoachTip, mountOptions)
    await showTip(wrapper)
    vi.advanceTimersByTime(6000)
    await nextTick()
    expect(wrapper.find('.coach-tip').exists()).toBe(false)
  })
})
```

- [ ] **Step 2: 运行确认失败**

Run: `npx vitest run src/tests/coachTipComponent.spec.ts`
Expected: FAIL——`Failed to resolve import "~/contentScripts/views/CoachTip.vue"`

- [ ] **Step 3: 实现组件**

创建 `src/contentScripts/views/CoachTip.vue`：

```vue
<!-- src/contentScripts/views/CoachTip.vue -->
<script setup lang="ts">
import { nextTick, onUnmounted, reactive, ref } from 'vue'
import { getMaxZIndex } from '../../logic/dom'
import { computeTooltipPosition } from '~/logic/tooltipPosition'
import type { AnchorRect } from '~/logic/tooltipPosition'
import { coachKeyLabel } from '~/logic/coachTip'
import { t } from '~/logic/i18n'

/**
 * 首次引导浮层（Coach Tip）：一次性提示「按住 Alt 划词即可标记」核心手势。
 * Spec: docs/superpowers/specs/2026-09-11-coach-tip-design.md
 *
 * 纯提示零交互：pointer-events-none，点击穿透宿主页。
 * 消失三通道（组件自管理，宿主零侵入）：6s 超时 / 任意 mousedown / 页面滚动。
 * 两阶段渲染：先隐藏挂载测量，定位完成后才可见（同 Tooltip 模式，防闪跳）。
 */
const DISMISS_TIMEOUT_MS = 6000

const visible = ref(false)
const isPositioned = ref(false)
const position = reactive({ x: 0, y: 0 })
const tipRef = ref<HTMLElement | null>(null)
const zIndex = ref(0)

const keyLabel = coachKeyLabel(/mac/i.test(navigator.platform))

let dismissTimer = 0

function dismiss() {
  hide()
}

async function show(anchorRect: AnchorRect) {
  zIndex.value = getMaxZIndex() + 100
  isPositioned.value = false
  visible.value = true
  await nextTick()
  const el = tipRef.value
  if (el) {
    const rect = el.getBoundingClientRect()
    const pos = computeTooltipPosition(
      anchorRect,
      { width: rect.width, height: rect.height },
      { width: window.innerWidth, height: window.innerHeight },
    )
    position.x = pos.x
    position.y = pos.y
  }
  isPositioned.value = true
  window.addEventListener('mousedown', dismiss, true)
  window.addEventListener('scroll', dismiss, true)
  dismissTimer = window.setTimeout(dismiss, DISMISS_TIMEOUT_MS)
}

function hide() {
  clearTimeout(dismissTimer)
  window.removeEventListener('mousedown', dismiss, true)
  window.removeEventListener('scroll', dismiss, true)
  visible.value = false
  isPositioned.value = false
}

onUnmounted(hide)

defineExpose({ show, hide })
</script>

<template>
  <Transition name="coach-tip-pop">
    <div
      v-if="visible"
      ref="tipRef"
      class="coach-tip fixed pointer-events-none select-none rounded-lg border border-gray-200 bg-white px-[12px] py-[8px] font-sans text-[12px] text-gray-600 shadow-xl dark:border-gray-700 dark:bg-gray-800 dark:text-gray-300"
      :class="{ 'coach-tip-in': isPositioned }"
      :style="{ top: `${position.y}px`, left: `${position.x}px`, zIndex, visibility: isPositioned ? 'visible' : 'hidden' }"
    >
      <span>{{ t('coachTip.hintPrefix') }}</span>
      <kbd class="mx-[4px] rounded-[4px] border border-amber-300 bg-amber-100 px-[6px] py-[2px] font-mono text-[12px] text-amber-800 dark:border-amber-700 dark:bg-amber-900/40 dark:text-amber-300">{{ keyLabel }}</kbd>
      <span>{{ t('coachTip.hintSuffix') }}</span>
    </div>
  </Transition>
</template>

<style scoped>
/* 同款 tooltip-pop-in 模式：动画绑定 isPositioned（两阶段渲染的测量期不播动画），
   keyframes 在 scoped 内定义（Shadow DOM 隔离，无法从外部样式复用）。 */
.coach-tip-in {
  animation: coach-tip-in 150ms ease-out;
}
@keyframes coach-tip-in {
  from {
    opacity: 0;
    transform: scale(0.96) translateY(4px);
  }
}
.coach-tip-pop-leave-active {
  transition: opacity 100ms ease-in, transform 100ms ease-in;
}
.coach-tip-pop-leave-to {
  opacity: 0;
  transform: scale(0.96) translateY(4px);
}
</style>
```

- [ ] **Step 4: 运行确认通过**

Run: `npx vitest run src/tests/coachTipComponent.spec.ts`
Expected: PASS（5 个用例全绿）

- [ ] **Step 5: 提交**

```powershell
git add src/contentScripts/views/CoachTip.vue src/tests/coachTipComponent.spec.ts; if ($?) { git commit -m "feat(coach-tip): CoachTip.vue 组件——两阶段定位/穿透提示/三通道自消失（TDD）" }
```

---

### Task 4: UIManager 挂载 + 学会即标记置位

**Files:**
- Modify: `src/contentScripts/ui.ts`（imports + 类字段 + `ensureMounted` + 新方法 + `createHighlight`）

- [ ] **Step 1: ui.ts 顶部 imports 与接口**

在 `import DisambiguationModal from './views/DisambiguationModal.vue'`（ui.ts:6）后加：

```ts
import CoachTip from './views/CoachTip.vue'
```

在与现有 imports 中（`import type { Candidate } from '~/logic/search'` ui.ts:23 后）加：

```ts
import type { AnchorRect } from '~/logic/tooltipPosition'
```

在 `export class UIManager {` 之前加接口定义：

```ts
export interface CoachTipInstance {
  show: (anchorRect: AnchorRect) => Promise<void>
  hide: () => void
}
```

- [ ] **Step 2: 类字段**

在 `private _tooltipDebounceTimer: number = 0`（ui.ts:27）后加：

```ts
  private _coachTipApp: CoachTipInstance | null = null
```

- [ ] **Step 3: ensureMounted 挂载第三 app**

在 modalRoot 的 `createApp({...}).mount(modalRoot)` 块（ui.ts:64-80）之后、`document.body.appendChild(newContainer)`（ui.ts:82）之前插入：

```ts
    const coachTipRoot = document.createElement('div')
    uiRoot.appendChild(coachTipRoot)
    this._coachTipApp = createApp(CoachTip).mount(coachTipRoot) as unknown as CoachTipInstance
```

- [ ] **Step 4: 公开方法**

在 `cancelTooltipDebounce()` 方法（ui.ts:280-282）后加：

```ts
  showCoachTip(anchorRect: AnchorRect): void {
    this.ensureMounted()
    this._coachTipApp?.show(anchorRect)
  }

  hideCoachTip(): void {
    this._coachTipApp?.hide()
  }
```

- [ ] **Step 5: 学会即标记置位**

`createHighlight` 末尾现有代码（ui.ts:425-426）：

```ts
    await sendMessage('add-mark', markData, 'background')
    // 记录本次新建标记选中的标签（含空集合），供下次新建时预选。仅新建分支，编辑分支不更新。
    settings.value.lastUsedTags = [...tags]
```

在其后追加：

```ts
    // 学会即标记：已成功使用 Alt 手势创建标记，无需再展示首次引导
    settings.value.coachTipDone = true
```

- [ ] **Step 6: 验证与提交**

Run: `npm run typecheck`; Run: `npm run lint`
Expected: 均 0 error（挂载属 DOM 接线，无新增单测；运行时验证见 Task 7）

```powershell
git add src/contentScripts/ui.ts; if ($?) { git commit -m "feat(coach-tip): UIManager 挂载 CoachTip 第三视图 + createHighlight 学会即标记置位" }
```

---

### Task 5: `processSelection` 触发接线

**Files:**
- Modify: `src/contentScripts/index.ts`（imports + `processSelection` 末尾分支）

- [ ] **Step 1: import 纯函数**

在 `import { highlightDefaultStyle, shortcuts } from '~/logic/config'`（index.ts:13）后加：

```ts
import { shouldShowCoachTip } from '~/logic/coachTip'
```

（`getRangyRangeRect` 已在 index.ts:12 导入，无需重复。）

- [ ] **Step 2: 末尾分支接入触发**

`processSelection` 末尾现有代码（index.ts:251-254）：

```ts
  state.tooltipApp?.hide()
  state.currentMarkIdForColorChange = null
  state.serializedSelection = null
  state.currentSerializationRoot = undefined
}
```

替换为：

```ts
  state.tooltipApp?.hide()
  state.currentMarkIdForColorChange = null
  state.serializedSelection = null
  state.currentSerializationRoot = undefined

  // 首次引导（Coach Tip）：划了词但未按 Alt 且从未引导过 → 选区旁一次性提示核心手势。
  // 显示即置位（先写标志再显示）：保证严格一次；storage 写失败最坏多显示一次，无害。
  if (shouldShowCoachTip({
    altKey: event.altKey,
    isCollapsed: initialSelection.isCollapsed,
    onMarkElement: markElement !== null,
    coachTipDone: settings.value.coachTipDone,
  })) {
    const range = initialSelection.rangeCount > 0 ? initialSelection.getRangeAt(0) : null
    const anchorRect = (range ? getRangyRangeRect(range) : null) ?? new DOMRect(event.clientX, event.clientY, 0, 0)
    settings.value.coachTipDone = true
    ui.showCoachTip(anchorRect)
  }
}
```

（说明：mousedown/scroll 隐藏通道已在组件内部监听 window，`handleMouseDown` 无需改动。）

- [ ] **Step 3: 验证与提交**

Run: `npm run typecheck`; Run: `npx vitest run src/tests/ui.spec.ts src/tests/state.spec.ts`
Expected: typecheck 0 error；既有用例全绿（无回归）

```powershell
git add src/contentScripts/index.ts; if ($?) { git commit -m "feat(coach-tip): processSelection 末尾分支触发一次性引导提示" }
```

---

### Task 6: Options「重新显示」入口

**Files:**
- Modify: `src/options/Options.vue`（script 加 handler + general 卡片加 UI 行）

- [ ] **Step 1: script 增加 handler**

在 `async function saveSettings()` 定义（Options.vue:86）前加：

```ts
function reshowCoachTip() {
  // 本地偏好即改即存：直接写全局 settings（storage 即时生效，content script 经
  // useWebExtensionStorage 监听自动同步）；上方 watch 也会把 localSettings 同步回来，
  // 无需走「保存设置」显式流程
  settings.value.coachTipDone = false
}
```

- [ ] **Step 2: general 卡片追加 UI 行**

在 `#general` 卡片内、theme `<select>` 的关闭标签 `</select>`（Options.vue:500）之后、卡片关闭 `</div>`（Options.vue:501）之前插入：

```html
          <p class="text-[14px] text-neutral-500 mt-[20px] mb-[4px]">
            {{ t('options.coachTipLabel') }}
          </p>
          <p class="text-[13px] text-neutral-400 mb-[12px]">
            {{ t('options.coachTipDesc') }}
          </p>
          <div class="flex items-center gap-[12px]">
            <span class="text-[13px] text-neutral-500">
              {{ settings.coachTipDone ? t('options.coachTipStatusShown') : t('options.coachTipStatusNotShown') }}
            </span>
            <button
              class="rounded-md bg-amber-500 px-3 py-1.5 text-xs font-medium text-gray-900 shadow-sm transition-colors hover:bg-amber-600 disabled:opacity-50"
              :disabled="!settings.coachTipDone"
              @click="reshowCoachTip"
            >
              {{ t('options.coachTipReshow') }}
            </button>
          </div>
```

- [ ] **Step 3: 验证与提交**

Run: `npm run typecheck`; Run: `npm run lint`
Expected: 均 0 error

```powershell
git add src/options/Options.vue; if ($?) { git commit -m "feat(coach-tip): Options 增加首次引导状态行与重新显示按钮" }
```

---

### Task 7: 全量验证 + 文档收尾

**Files:**
- Modify: `docs/NIT_ROADMAP.md:61`（§3 条目）与 `:113`（§6 Active Work）
- Modify: `.project/ops_changelog.md`（追加 FEAT/DOCS 行）

- [ ] **Step 1: Layer 1+2 自动化证据**

Run: `npx vitest run`
Expected: 全量 PASS（既有 25+ spec + 新增 2 spec 零失败）

Run: `npm run lint`
Expected: 0 error / 0 warning（exit 0）

Run: `npm run typecheck`
Expected: 0 error

Run: `npm run build`
Expected: exit 0（构建通过，CoachTip 打进 contentScripts bundle）

- [ ] **Step 2: Layer 3 运行时验证（Driver 手动，Agent 呈现清单）**

构建产物手动加载（`npm run dev` 或 `npm run build` 后加载 `extension/`），全新 profile 验证：

- [ ] 首次普通划词（不按 Alt）→ 选区下方出现提示条「按住 Alt 划词即可标记」
- [ ] 提示条不拦截点击（点击页面任意处立即消失且点击命中原目标）
- [ ] 6s 不操作自动消失；划词后滚动页面立即消失
- [ ] 再次普通划词 → 不再出现（严格一次）
- [ ] 按 Alt+划词创建标记成功 → 标志置位（Options 状态显示「已显示」）
- [ ] Options → 通用设置 →「重新显示」→ 下次普通划词再次出现一次
- [ ] 暗黑模式下提示条为 gray-800 底 + amber-300 键帽；Mac 环境（如有）键帽显示「⌥ Option」

- [ ] **Step 3: NIT_ROADMAP 状态刷新**

`docs/NIT_ROADMAP.md` §3 第 61 行条目「页面内首次引导浮层（Coach Tip）」：`⭐⭐⭐⭐⭐` 列改为 `[已完成]`，评估理由末尾追加实现说明（CoachTip.vue + coachTipDone 标志 + Options 重显入口）。
§6 第 113 行状态「待办（P0）」改为「已完成」。

- [ ] **Step 4: ops_changelog 登记**

`.project/ops_changelog.md` 表末追加（时间用实际执行时间，Commit_ID 用实际 hash）：

```markdown
| 2026-09-11T<HH:MM:SS>+08:00 | FEAT | src/logic/coachTip.ts + src/contentScripts/views/CoachTip.vue + src/contentScripts/{ui,index}.ts + src/options/Options.vue + i18n locales | 首次未按 Alt 划词时选区旁一次性提示「按住 Alt 划词即可标记」；显示即置位/Alt 成功标记即置位；Options 提供重新显示入口。Spec: docs/superpowers/specs/2026-09-11-coach-tip-design.md | <hash> | git checkout HEAD -- src/logic/coachTip.ts src/contentScripts/ src/options/Options.vue src/logic/i18n/locales/ |
| 2026-09-11T<HH:MM:SS>+08:00 | DOCS | docs/NIT_ROADMAP.md | Coach Tip 条目转正 [已完成]，Active Work 状态刷新 | <hash> | git checkout HEAD -- docs/NIT_ROADMAP.md |
```

- [ ] **Step 5: 提交文档**

```powershell
git status; git add docs/NIT_ROADMAP.md .project/ops_changelog.md; if ($?) { git commit -m "docs: NIT_ROADMAP Coach Tip 条目转正 + ops_changelog 登记" }
```

（`git status` 全量核对后再 add，防路径遗漏。）

---

## Self-Review 记录

- **Spec 覆盖**：§3.1 触发→Task 1/5；§3.2 组件/挂载→Task 3/4；§3.3 消失三通道→Task 3（组件自管理）；§3.4 settings→Task 2；§3.5 Options→Task 6；§3.6 i18n→Task 2（prefix/suffix 细化已声明）；§5 边界→条件由纯函数覆盖（Task 1 测试）+ Layer 3 清单（Task 7 Step 2）；§6 测试→Task 1/3。无缺口。
- **占位符扫描**：Task 7 Step 4 的 `<HH:MM:SS>`/`<hash>` 为执行期动态值占位，属合法（执行时替换为实际值），其余步骤均含完整代码。
- **类型一致性**：`CoachTipCondition` 四字段（Task 1）与 Task 5 调用逐字对应；`CoachTipInstance.show(anchorRect: AnchorRect) / hide()`（Task 4）与 Task 3 `defineExpose({ show, hide })` 一致；`coachTipDone` 字段名在 Task 2/4/5/6 全程一致；i18n key `coachTip.hintPrefix/hintSuffix`、`options.coachTip*` 在 Task 2/3/6 一致。
